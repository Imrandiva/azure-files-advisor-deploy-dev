# Copy to .env (or set these as app settings) to pre-fill the advisor for your team.
# All are optional — users can also type them into the UI.
PORT=8080
DEFAULT_SUBSCRIPTION_ID=
DEFAULT_RESOURCE_GROUP=
DEFAULT_STORAGE_ACCOUNT=
